{{#if isViewingNestedViews }}
  {{#component-column columnType='navigation'}}
    {{listed-reporting-coders currentCollection=currentCoders }}
  {{/component-column }}
{{ else }}
  {{listed-reporting-coders-full currentCollection=currentCoders }}
{{/if}}
