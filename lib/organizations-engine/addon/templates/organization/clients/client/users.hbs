{{#if currentApp.isNotInFullScreenView }}
  {{organization-client-users currentUsers=currentUsers }}
  {{#if isViewingListFilters }}

  {{ else }}
    {{outlet}}
  {{/if}}
{{ else }}
  {{outlet}}
{{/if}}
