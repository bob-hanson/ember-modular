{{#if currentApp.isNotInFullScreenView }}
  {{organization-users currentOrganization=currentOrganization currentUsers=currentUsers }}
  {{#if isViewingListFilters }}

  {{ else }}
    {{outlet}}
  {{/if}}
{{ else }}
  {{outlet}}
{{/if}}
