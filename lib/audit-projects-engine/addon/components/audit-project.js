// import Ember from 'ember';
import BaseComponent from 'hyspa-base-components/components/base-component';

export default BaseComponent.extend({
  classNames: ['audit-project'],
  classNameBindings: ['colSpan'],
  percentageWidth: 80
});
