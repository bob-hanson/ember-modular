import { moduleFor, test } from 'ember-qunit';

moduleFor('route:domains/domain/sub-domains/sub-domain/categories/category/sub-categories', 'Unit | Route | domains/domain/sub domains/sub domain/categories/category/sub categories', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
