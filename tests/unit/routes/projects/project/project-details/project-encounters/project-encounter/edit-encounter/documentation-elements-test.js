import { moduleFor, test } from 'ember-qunit';

moduleFor('route:projects/project/project-details/project-encounters/project-encounter/edit-encounter/documentation-elements', 'Unit | Route | projects/project/project details/project encounters/project encounter/edit encounter/documentation elements', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
