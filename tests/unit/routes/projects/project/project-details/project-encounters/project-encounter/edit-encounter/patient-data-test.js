import { moduleFor, test } from 'ember-qunit';

moduleFor('route:projects/project/project-details/project-encounters/project-encounter/edit-encounter/patient-data', 'Unit | Route | projects/project/project details/project encounters/project encounter/edit encounter/patient data', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
