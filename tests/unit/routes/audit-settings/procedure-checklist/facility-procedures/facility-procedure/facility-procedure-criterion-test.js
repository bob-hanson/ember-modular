import { moduleFor, test } from 'ember-qunit';

moduleFor('route:audit-settings/procedure-checklist/facility-procedures/facility-procedure/facility-procedure-criterion', 'Unit | Route | audit settings/procedure checklist/facility procedures/facility procedure/facility procedure criterion', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
