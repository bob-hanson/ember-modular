import { moduleFor, test } from 'ember-qunit';

moduleFor('route:audit-settings/audit-guidelines/audit-guideline/edit-audit-guideline/edit-scoring-methods', 'Unit | Route | audit settings/audit guidelines/audit guideline/edit audit guideline/edit scoring methods', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
