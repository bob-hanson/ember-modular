import { moduleFor, test } from 'ember-qunit';

moduleFor('route:audit-settings/audit-guidelines/audit-guideline/edit-audit-guideline/edit-audit-scope', 'Unit | Route | audit settings/audit guidelines/audit guideline/edit audit guideline/edit audit scope', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
