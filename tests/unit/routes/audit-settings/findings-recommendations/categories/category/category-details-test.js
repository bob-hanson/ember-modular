import { moduleFor, test } from 'ember-qunit';

moduleFor('route:audit-settings/findings-recommendations/categories/category/category-details', 'Unit | Route | audit settings/findings recommendations/categories/category/category details', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
